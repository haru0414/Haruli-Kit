{
  "name": "auth",
  "displayName": "Authentication",
  "description": "認證與授權解決方案",
  "icon": "🔐",
  "variants": {
    "nextjs": {
      "description": "Next.js 專案使用 NextAuth",
      "packages": {
        "dependencies": [
          "next-auth",
          "@auth/prisma-adapter"
        ]
      }
    },
    "express": {
      "description": "Express 專案使用 JWT",
      "packages": {
        "dependencies": [
          "jsonwebtoken",
          "bcryptjs"
        ],
        "devDependencies": [
          "@types/jsonwebtoken",
          "@types/bcryptjs"
        ]
      }
    }
  },
  "notes": "NextAuth 需要設定 NEXTAUTH_SECRET；JWT 需要設定 JWT_SECRET"
}
